# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# runtime / uploads
uploads/
*.log

# secrets & env (never commit real secrets) — ignore every .env* EXCEPT the templates
.env
.env.*
!.env.example
!.env.*.example

# tooling
coverage/
.DS_Store
